{
  "name": "with-typescript",
  "version": "1.0.0",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start -p $PORT",
    "h_build": "docker rmi -f zapad-pet-frontend && docker build . -t zapad-pet-frontend && heroku container:push web --app zapad-pet-frontend && heroku container:release web --app zapad-pet-frontend",
    "h_logs": "heroku logs --tail --app zapad-pet-frontend",
    "type-check": "tsc"
  },
  "dependencies": {
    "next": "latest",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.16",
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "typescript": "^4.1.3"
  },
  "license": "MIT"
}
